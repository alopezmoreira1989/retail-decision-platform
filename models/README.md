# models

Analytical, statistical, and ML models that read Gold marts and emit structured findings (availability, underperformance, inventory anomaly, assortment fit, demand estimate). Deterministic baselines and learned models both live here, evaluated against each other. See [../docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md#detection--inference).
