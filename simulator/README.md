# simulator

Hidden world-state generation and the retailer-specific observation process that derives RAW feeds from it.

Owns the ground truth used only by the evaluation harness — never imported by ingestion, transformations, models, or recommendations. See [../docs/SIMULATION.md](../docs/SIMULATION.md).
